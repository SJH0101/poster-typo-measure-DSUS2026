# 상자 긋기 도구

`tool.html`(1.1 MB)은 이 저장소에서 뺐다. 도구 안에 포스터 썸네일 200장이 base64 로 박혀 있는데,
실물 포스터 이미지는 eMuseum 이용 조건을 확인하기 전까지 공개하지 않는다.

- 원본은 연구자 컴퓨터의 `~/Documents/poster/labeler_tool_archive/tool_20260920.html` 에 있다.
- 도구가 낸 상자 자료는 `boxes/human_v2.json` 에 좌표로 들어 있다 (이미지 없음).
- 가이드 긋기 도구(2.0)의 원본 틀은 `docs/labeling/tool/template.html` 이다 — 여기에는 이미지가 없고,
  `docs/labeling/tool/build.py` 가 판 목록과 이미지를 넣어 만든다.
