# 논문 재현에 필요한 것만 둔다.

# 재는 쪽 (measure/) · 규칙 (rules.py) · 채점 (eval/)
numpy>=1.24
scipy>=1.10
Pillow>=10.0

# 줄 검출 — 파이프라인의 «찾기». eval/synth_score.py --remeasure · measure_corpus.py 가 부른다.
# 논문 수치는 0.22.1 로 냈다 (docs/synth_result.json 의 측정 provenance).
surya-ocr==0.22.1

# 기울기 판정 (measure_corpus.measure_items skew=True) 과 검출기 비교 (detector_compare.py).
# 합성 실험은 skew=False 라 이것 없이도 돈다.
easyocr>=1.7
